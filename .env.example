# Copy to .env and fill in after deploying the Supabase backend (see README step 5).
# Both are PUBLIC values (the anon key is safe in a browser). Leave blank to run
# in offline mode (CSV import / manual logging / demo) with Strava disabled.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
